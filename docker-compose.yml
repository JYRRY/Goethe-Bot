services:
  bot:
    build: .
    container_name: jyry-ai-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-db:/app/db
    environment:
      - DATABASE_PATH=/app/db/goethe_bot.db
      - LOG_LEVEL=INFO

volumes:
  bot-db:
