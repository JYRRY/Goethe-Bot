services:
  bot:
    build: .
    container_name: jyry-ai-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/app/data
    environment:
      - DATABASE_PATH=/app/data/goethe_bot.db
      - LOG_LEVEL=DEBUG

volumes:
  bot-data:
